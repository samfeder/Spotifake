<h1>Bands | Spotifake</h1>

<h2><%= @band.name %></h2>
<% @band.albums.each do |album| %>
  <%=link_to album.name, album_url(album) %><br>
<% end %>
<br><br>
<%=link_to "Add Album", new_band_album_url(@band) %>
<br><br><br><br>
<%=link_to "All bands", bands_url %><br><br>
<%= button_to "Delete #{@band.name}", band_url(@band), method: :delete%>